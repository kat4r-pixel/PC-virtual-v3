name: RDP - Windows PT-BR

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:

      # ============================================================
      # 1. IDIOMA E REGIÃO
      # ============================================================
      - name: 🇧🇷 Configurar Windows em Português do Brasil
        shell: powershell
        run: |
          Write-Host "========================================"
          Write-Host " CONFIGURANDO WINDOWS EM PORTUGUÊS"
          Write-Host "========================================"

          try {
            Install-Language pt-BR -CopyToSettings -ErrorAction Stop
          }
          catch {
            Write-Host "Pacote de idioma não pôde ser instalado completamente."
          }

          try {
            $idioma = New-WinUserLanguageList pt-BR

            Set-WinUserLanguageList $idioma -Force
            Set-WinUILanguageOverride pt-BR
            Set-Culture pt-BR
            Set-WinHomeLocation -GeoId 32

            Write-Host "Português do Brasil configurado."
          }
          catch {
            Write-Host "Algumas configurações de idioma exigirão novo logon."
          }


      # ============================================================
      # 2. DIAGNÓSTICO INICIAL
      # ============================================================
      - name: 📊 Diagnóstico do computador
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "========================================" -ForegroundColor Cyan
          Write-Host " DIAGNÓSTICO DO RUNNER" -ForegroundColor Cyan
          Write-Host "========================================" -ForegroundColor Cyan

          $cpu = Get-CimInstance Win32_Processor
          $ram = Get-CimInstance Win32_ComputerSystem

          $ramGB = [math]::Round(
            $ram.TotalPhysicalMemory / 1GB,
            2
          )

          Write-Host "CPU: $($cpu.Name)"
          Write-Host "Núcleos: $($cpu.NumberOfCores)"
          Write-Host "Threads: $($cpu.NumberOfLogicalProcessors)"
          Write-Host "RAM: $ramGB GB"

          Write-Host ""
          Write-Host "DISCOS:"
          Get-PSDrive -PSProvider FileSystem |
            Select-Object Name,
              @{N="UsadoGB";E={[math]::Round($_.Used/1GB,2)}},
              @{N="LivreGB";E={[math]::Round($_.Free/1GB,2)}} |
            Format-Table -AutoSize


      # ============================================================
      # 3. LIMPEZA SEGURA
      # ============================================================
      - name: 🧹 Fazer limpeza segura
        shell: powershell
        run: |
          Write-Host "========================================"
          Write-Host " LIMPEZA DO WINDOWS"
          Write-Host "========================================"

          $pastas = @(
            "$env:TEMP\*",
            "$env:LOCALAPPDATA\Temp\*"
          )

          foreach ($pasta in $pastas) {
            try {
              Remove-Item `
                $pasta `
                -Recurse `
                -Force `
                -ErrorAction SilentlyContinue
            }
            catch {}
          }

          # Limpar cache de miniaturas
          try {
            Remove-Item `
              "$env:LOCALAPPDATA\Microsoft\Windows\Explorer\thumbcache*" `
              -Force `
              -ErrorAction SilentlyContinue
          }
          catch {}

          # Limpeza do componente do Windows
          try {
            DISM.exe `
              /Online `
              /Cleanup-Image `
              /StartComponentCleanup
          }
          catch {}

          Write-Host "Limpeza concluída." -ForegroundColor Green


      # ============================================================
      # 4. CONFIGURAÇÕES BÁSICAS DE DESEMPENHO
      # ============================================================
      - name: ⚡ Otimizar configurações do Windows
        shell: powershell
        run: |
          Write-Host "Configurando desempenho..."

          # Desativar alguns efeitos visuais desnecessários
          try {
            Set-ItemProperty `
              "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" `
              -Name VisualFXSetting `
              -Type DWord `
              -Value 2 `
              -Force
          }
          catch {}

          # Ativar plano de energia de alto desempenho, se disponível
          try {
            powercfg /setactive SCHEME_MIN
          }
          catch {}

          Write-Host "Configurações aplicadas."


      # ============================================================
      # 5. INSTALAR FERRAMENTAS
      # ============================================================
      - name: 🛠️ Instalar ferramentas de desenvolvimento
        shell: powershell
        run: |
          Write-Host "Instalando ferramentas..."

          # Git
          if (-not (Get-Command git -ErrorAction SilentlyContinue)) {
            winget install `
              --id Git.Git `
              --silent `
              --accept-package-agreements `
              --accept-source-agreements `
              2>$null
          }

          # Node.js LTS
          if (-not (Get-Command node -ErrorAction SilentlyContinue)) {
            winget install `
              --id OpenJS.NodeJS.LTS `
              --silent `
              --accept-package-agreements `
              --accept-source-agreements `
              2>$null
          }

          Write-Host "Ferramentas verificadas."


      # ============================================================
      # 6. VERIFICAR DIRECTX E GPU
      # ============================================================
      - name: 🎮 Verificar GPU e DirectX
        shell: powershell
        run: |
          Write-Host "========================================"
          Write-Host " GPU / DIRECTX"
          Write-Host "========================================"

          try {
            Get-CimInstance Win32_VideoController |
              Select-Object Name,
                DriverVersion,
                VideoModeDescription |
              Format-List
          }
          catch {
            Write-Host "Não foi possível obter informações da GPU."
          }

          Write-Host ""
          Write-Host "DirectX:"
          try {
            dxdiag /t "$env:TEMP\dxdiag.txt"

            if (Test-Path "$env:TEMP\dxdiag.txt") {
              Select-String `
                -Path "$env:TEMP\dxdiag.txt" `
                -Pattern "DirectX Version"
            }
          }
          catch {
            Write-Host "Diagnóstico do DirectX indisponível."
          }


      # ============================================================
      # 7. PREPARAR ARMAZENAMENTO
      # ============================================================
      - name: 💾 Preparar armazenamento
        shell: powershell
        run: |
          Write-Host "========================================"
          Write-Host " ARMAZENAMENTO"
          Write-Host "========================================"

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "D:\Projetos" |
            Out-Null

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "D:\Roblox" |
            Out-Null

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "D:\Downloads" |
            Out-Null

          Get-PSDrive -PSProvider FileSystem |
            Select-Object Name,
              @{N="LivreGB";E={[math]::Round($_.Free/1GB,2)}} |
            Format-Table


      # ============================================================
      # 8. CONFIGURAR RDP
      # ============================================================
      - name: 🖥️ Configurar Área de Trabalho Remota
        shell: powershell
        run: |
          Write-Host "Configurando RDP..."

          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" `
            -Name "fDenyTSConnections" `
            -Value 0 `
            -Force

          # Autenticação de nível de rede
          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "UserAuthentication" `
            -Value 1 `
            -Force

          # TLS
          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "SecurityLayer" `
            -Value 1 `
            -Force

          netsh advfirewall firewall delete rule `
            name="RDP-Tailscale" 2>$null

          netsh advfirewall firewall add rule `
            name="RDP-Tailscale" `
            dir=in `
            action=allow `
            protocol=TCP `
            localport=3389

          Restart-Service TermService -Force

          Write-Host "RDP ativado."


      # ============================================================
      # 9. CRIAR USUÁRIO
      # ============================================================
      - name: 🔑 Criar usuário RDP
        shell: powershell
        run: |
          $maiusculas = [char[]](65..90)
          $minusculas = [char[]](97..122)
          $numeros = [char[]](48..57)
          $especiais = [char[]]@(33,35,36,37,38,42,43,45,61,64)

          $senhaArray = @()

          $senhaArray += $maiusculas | Get-Random -Count 4
          $senhaArray += $minusculas | Get-Random -Count 4
          $senhaArray += $numeros | Get-Random -Count 4
          $senhaArray += $especiais | Get-Random -Count 4

          $senha = -join (
            $senhaArray |
            Sort-Object { Get-Random }
          )

          "::add-mask::$senha"

          $secure = ConvertTo-SecureString `
            $senha `
            -AsPlainText `
            -Force

          if (-not (Get-LocalUser `
              -Name RDP `
              -ErrorAction SilentlyContinue)) {

            New-LocalUser `
              -Name RDP `
              -Password $secure `
              -AccountNeverExpires
          }

          Add-LocalGroupMember `
            -Group "Remote Desktop Users" `
            -Member RDP `
            -ErrorAction SilentlyContinue

          "SENHA_RDP=$senha" |
            Out-File `
              $env:GITHUB_ENV `
              -Append

          Write-Host "Usuário RDP criado."


      # ============================================================
      # 10. INSTALAR TAILSCALE
      # ============================================================
      - name: 🌐 Instalar Tailscale
        shell: powershell
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $arquivo = "$env:TEMP\tailscale.msi"

          Invoke-WebRequest `
            -Uri $url `
            -OutFile $arquivo `
            -UseBasicParsing

          Start-Process `
            msiexec.exe `
            -ArgumentList `
              "/i",
              "`"$arquivo`"",
              "/quiet",
              "/norestart" `
            -Wait

          Remove-Item `
            $arquivo `
            -Force

          Write-Host "Tailscale instalado."


      # ============================================================
      # 11. CONECTAR TAILSCALE
      # ============================================================
      - name: 🔗 Conectar ao Tailscale
        shell: powershell
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $ts =
            "$env:ProgramFiles\Tailscale\tailscale.exe"

          & $ts up `
            --authkey="$env:TAILSCALE_AUTH_KEY" `
            --hostname="gh-rdp-$env:GITHUB_RUN_ID"

          $ip = $null

          for ($i = 0; $i -lt 15; $i++) {

            Start-Sleep -Seconds 4

            $ip =
              & $ts ip -4 2>$null

            if ($ip) {
              break
            }
          }

          if (-not $ip) {
            Write-Error "Tailscale não recebeu um endereço IP."
            exit 1
          }

          "IP_TAILSCALE=$ip" |
            Out-File `
              $env:GITHUB_ENV `
              -Append

          Write-Host "IP Tailscale: $ip"


      # ============================================================
      # 12. TESTAR RDP
      # ============================================================
      - name: 🔎 Testar RDP
        shell: powershell
        run: |
          $teste =
            Test-NetConnection `
              -ComputerName $env:IP_TAILSCALE `
              -Port 3389

          if (-not $teste.TcpTestSucceeded) {
            Write-Error "RDP não respondeu na porta 3389."
            exit 1
          }

          Write-Host "RDP funcionando." -ForegroundColor Green


      # ============================================================
      # 13. RELATÓRIO FINAL
      # ============================================================
      - name: 📋 Relatório final
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================" -ForegroundColor Cyan
          Write-Host "             RDP PRONTO" -ForegroundColor Cyan
          Write-Host "============================================" -ForegroundColor Cyan

          Write-Host "IP Tailscale : $env:IP_TAILSCALE"
          Write-Host "Usuário      : RDP"

          Write-Host ""
          Write-Host "FERRAMENTAS:"
          
          if (Get-Command git -ErrorAction SilentlyContinue) {
            Write-Host "Git          : OK" -ForegroundColor Green
          }
          else {
            Write-Host "Git          : NÃO ENCONTRADO" -ForegroundColor Yellow
          }

          if (Get-Command node -ErrorAction SilentlyContinue) {
            Write-Host "Node.js      : OK" -ForegroundColor Green
            Write-Host "Node versão  : $(node --version)"
          }
          else {
            Write-Host "Node.js      : NÃO ENCONTRADO" -ForegroundColor Yellow
          }

          Write-Host ""
          Write-Host "IDIOMA       : Português (Brasil)"
          Write-Host "REGIÃO       : Brasil"

          Write-Host ""
          Write-Host "PASTAS:"
          Write-Host "D:\Projetos"
          Write-Host "D:\Roblox"
          Write-Host "D:\Downloads"

          Write-Host ""
          Write-Host "============================================"


      # ============================================================
      # 14. MANTER RUNNER ATIVO
      # ============================================================
      - name: ⏱️ Manter sessão ativa
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "RDP está ativo." -ForegroundColor Green
          Write-Host "Mantenha esta execução do GitHub Actions aberta."
          Write-Host ""

          $minutos = 0

          while ($true) {
            Start-Sleep -Seconds 60
            $minutos++

            Write-Host `
              "Sessão ativa: $minutos minuto(s)" `
              -ForegroundColor DarkGray
          }
